# ─── Phase-5 Auto-Annotation Dependencies (OPTIONAL extra) ─────────────────────
# Install: pip install -r requirements.txt -r requirements-annotation.txt
#
# NOT installed in CI (ADR-P5-11): unit tests use the registered FakeAnnotator;
# real-backend tests are gated behind pytest.importorskip and run only on
# machines with this extra installed (the local GPU box).
#
# The primary yolo_world backend needs ONLY ultralytics (already a core
# dependency) plus the CLIP text encoder below. GroundingDINO (optional,
# disabled by default in configs/annotation.yaml) needs transformers.

# GroundingDINO second-opinion backend (configs/annotation.yaml enables it
# only if measured yolo_world precision on priority classes is < 0.4).
transformers>=4.45,<5.0

# YOLO-World's set_classes() text encoder. Ultralytics auto-installs this at
# runtime when missing — pin it here instead so offline/deterministic runs
# never trigger a surprise install. Pinned at M1 provisioning (2026-07-17,
# local GPU box) to the CLIP fork's HEAD at that time.
clip @ git+https://github.com/ultralytics/CLIP.git@a57ec09a1668b6a5905ff323d734701f8d11d0e2

# Image IO for the annotation stages (transitively present via ultralytics;
# listed for explicitness, mirroring the psutil precedent in requirements.txt).
Pillow>=10.0,<12.0
