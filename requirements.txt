# ─── Core Runtime Dependencies ────────────────────────────────────────────────
# Locked versions for reproducible environments
# Platform: Python 3.10+ on Linux (Raspberry Pi 5) / Android / macOS / Windows

# Object Detection
ultralytics>=8.3,<9.0
opencv-python>=4.9,<5.0
numpy>=1.26,<2.0

# Configuration
PyYAML>=6.0,<7.0

# Audio playback (TTS output)
sounddevice>=0.4.6,<0.5

# ─── SmolVLM2 (Optional — install only if hardware supports it) ───────────────
# Install: pip install -r requirements.txt -r requirements-smolvlm.txt
# transformers>=4.40,<5.0
# torch>=2.2,<3.0
# Pillow>=10.0,<11.0
# accelerate>=0.27,<0.28

# ─── Development Tools ────────────────────────────────────────────────────────
black>=24.0,<25.0
ruff>=0.4,<1.0
mypy>=1.10,<2.0
types-PyYAML>=6.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# ─── MLOps ────────────────────────────────────────────────────────────────────
dvc>=3.50,<4.0
