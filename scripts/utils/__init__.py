"""
scripts.utils — Shared Utility Scripts
=======================================

Shared utility scripts used by dataset, QA, and training scripts.

Script inventory:
    scaffold_dirs.ps1       — Create project directory structure (Stage 1)
    visualize_detections.py — Draw bounding boxes on frames for debugging
    convert_formats.py      — Convert between YOLO/COCO/Pascal VOC label formats
    check_environment.py    — Verify Python, CUDA, camera availability
    download_piper_models.py — Download Piper TTS voice model files
"""
